const express = require('express');
const cors = require('cors');
//const ws = 
const fs = require('fs');

const PORT = 5500;
// const app = express();

// app.use(cors());


// app.get('/getImage', async (req, res) => {
//   const buffer = await fs.readFileSync('image.jpg');
//   console.log('got buffer', buffer);

//   res.send(buffer);
// })



// app.listen(PORT, () => {
//   console.log(`app is listening on port ${PORT}`);
// })